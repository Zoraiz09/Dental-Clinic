# Copy to .env and fill in to connect the app to a real Supabase project.
# Leave blank to run in MOCK mode (in-memory demo data, no network).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
